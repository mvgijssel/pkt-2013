<%= form_tag do %>

    <%= label_tag(:temperatuur, "Temperatuur:") %>
    <%= text_field_tag(:temperatuur) %>
    <%= hidden_field_tag(:tag, :initieel) %>
    <%= submit_tag("Verstuur") %>

<% end %>