<%

   fact = answer.fact
   fact.possible_values.each_with_index do |option, n|

%>

    <%= radio_button_tag(fact.name, option[:value], n == 0) %>
    <%= label_tag("#{fact.name}_#{option[:value]}", option[:label]) %>

<% end %>